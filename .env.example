# ===========================================
# FlowCode Environment Variables (Railway)
# ===========================================
# Add these in Railway's Settings → Variables

# Server Configuration
NODE_ENV=production
# PORT is auto-set by Railway

# MongoDB Connection
# Get a free MongoDB Atlas cluster at https://www.mongodb.com/cloud/atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/flowcode

# JWT Configuration
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret-min-32-chars
JWT_REFRESH_SECRET=your-super-secure-refresh-secret-min-32-chars

# Optional: Judge0 API (for additional language support)
# Without this, code runs locally (Python, JS, TS only)
# JUDGE0_API_KEY=your-rapidapi-key
